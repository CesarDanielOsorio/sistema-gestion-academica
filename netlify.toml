# Configuración de despliegue en Netlify
[build]
  command = "npm run build"
  publish = "dist"

# SPA: redirige todas las rutas a index.html para que React Router funcione
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
